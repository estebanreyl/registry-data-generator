# Values file for registry scenario: acr-50l-100mb
# 50 layers, 100MB each, 5000MB total image size
# 3,145 images for ~15TB total

env:
  # Number of Docker images to create
  numberOfImages: 3145

  # Number of layers per image  
  numberOfLayers: 50

  # Size of each layer (in KB) - 100MB = 102400KB
  sizeOfLayersKb: '102400'

  # Number of upload threads
  numOfThreads: 10

  # Registry URI - update with your registry
  registryUri: your-registry.azurecr-test.io

  # Set --insecure-registry flag
  insecureRegistry: false

  # Docker user - update with your credentials
  dockerUser: ""

  # Docker password - update with your credentials  
  dockerPassword: ""

  # Directory path in the registry
  repoPath: docker-auto

  # Remove images once done
  removeImages: false

  # Image tag
  tag: 1

  # Debug output
  debug: false

resources:
  requests:
    cpu: 1
    memory: 6Gi
  limits:
    cpu: 4
    memory: 16Gi
